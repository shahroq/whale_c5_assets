/**
 * Whale Assets: Initiate Popover
 */

const popover = function () {
  const version = '2.0.0';
  // console.log(`popover version ${version}`);

  $('[data-toggle="popover"]').popover();
};

export { popover };
